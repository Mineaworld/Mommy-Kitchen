# Khmer Recipe App

Mobile-first PWA for Khmer recipe videos with admin-managed content.

## Setup

1. Copy `.env.example` to `.env.local`
2. Fill Supabase values
3. Run `npm install`
4. Run `npm run dev`

## Build

`npm run build`
